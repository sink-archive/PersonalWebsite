@model string[]

<div class="card">
	<div class="card-body">
		<h5 class="card-title">@Model[0]</h5>
		@if (Model.Length >= 4)
		{
			<h6 class="card-subtitle">@Model[3]</h6>
		}
		<a href="@Model[1]/@Model[2]"
		   class="card-link stretched-link">Visit page</a>
	</div>
</div>